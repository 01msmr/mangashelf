services:
  mangashelf:
    build: .
    image: mangashelf
    container_name: mangashelf
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - mangashelf-data:/code/data
    env_file:
      - .env

volumes:
  mangashelf-data:
